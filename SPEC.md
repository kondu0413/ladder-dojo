# ラダー図学習アプリ — プロジェクトナレッジ

このファイルはリポジトリ直下に置く、プロジェクトの唯一の正典(single source of truth)である。
Claude Code はこのファイルを読み、ここに書かれた機能仕様・フェーズ計画・運用ルールに従って自律的に開発を進める。
このファイルと矛盾する判断が必要になった場合は、勝手に仕様を変えず `docs/DECISIONS.md` に記録して人間に確認を求める。

---

## 1. プロジェクト概要

- 名称: ラダー図学習アプリ(仮)
- 目的: PLCのラダー図(ラダーロジック)を「読める」→「直せる」→「書ける」の順に段階的に身につけられるWebアプリ
- 中核: ブラウザ上で動くラダーシミュレータ。すべての学習モードはこのシミュレータの上に載る
- 一人で学ぶだけでなく、問題投稿・ランキング・管理者ビューによって複数人で使えるサービスを目指す
- 対象ユーザー: 当面は「製造現場に入ったばかりの新人・若手(電気の基礎は薄い)」を仮ペルソナとする。後に一般学習者・企業導入へ広げられる設計にする

---

## 2. 開発体制・運用ルール

### 2.1 自律サイクル
- 開発はClaude Codeが自律的に行う。人間の介入は「成果物の確認」と「機能仕様の判断」のみ
- フェーズ1 → フェーズ2 → フェーズ3 の順に進める。各フェーズの完了条件(DoD)を満たしたら、人間の承認を待たずに次フェーズへ自動的に進んでよい
- 各フェーズ内では「実装 → テスト → 自己レビュー → 修正」のループを、DoDを満たすまで回す
- 技術的な選択・設計・リファクタリングはClaude Codeの裁量。人間に聞かない
- 機能仕様に関わる判断(この機能は入れるか、挙動はどうあるべきか)で本ファイルに答えがない場合のみ、`docs/DECISIONS.md` に「論点・選択肢・暫定採用案・理由」を書き、暫定案で進める。人間は後から差し戻せる

### 2.2 進捗の可視化(人間が確認するための成果物)
- `docs/PROGRESS.md`: フェーズごとの進捗、完了したDoD項目、次にやること。毎サイクル更新する
- `docs/DECISIONS.md`: 仕様上の暫定判断・技術上の重要判断のログ(日付付き)
- `docs/COST.md`: 現在のランニングコストの内訳と、課金が必要と判断した場合の提案(§2.3参照)
- `README.md`: セットアップ手順、ローカル起動方法、デプロイ方法。人間が動作確認できる状態を常に保つ
- 各フェーズ完了時に、人間が触って確認できるデプロイ済みURL(またはローカル起動手順)を `docs/PROGRESS.md` に明記する

### 2.3 コスト原則
- ランニングコストは **基本0円** で設計する(無料枠のホスティング・DB・認証を使う)
- 技術的課題や完成度の問題で有料サービスが必要と判断した場合は、実装せずに `docs/COST.md` に「何のために・月額いくら・無料の代替案とその欠点」を書いて提案する。人間の承認後に採用する
- 無料枠の上限(リクエスト数・ストレージ・同時接続など)を `docs/COST.md` に記載し、上限に近づいた場合の縮退方針も書く

### 2.4 品質基準
- シミュレータのロジックはユニットテストで網羅する(ラダーの評価順序、タイマ・カウンタの挙動、自己保持など)
- 問題の判定ロジックはテスト必須。誤判定は学習アプリとして致命的
- 主要な画面フローはE2Eテストを持つ
- CIでテストが通らない状態で次の作業に進まない

---

## 3. 機能仕様(確定事項)

### 3.1 ラダーシミュレータ(すべての土台)
- ラダー図をブラウザ上で表示し、入力(ボタン・センサなど)をタップ/クリックで操作すると、リアルタイムに回路が評価され、出力(ランプ・モータなど)の状態が変わる
- 通電している接点・線は視覚的に強調表示する(ON/OFFが一目で分かること)
- スキャン方式はPLCと同じ「上から下・左から右、1スキャンで全ラングを評価、出力は次スキャンに反映」を模擬する
- タイマは実時間で動く(学習用に速度調整できるとよい)
- 対応命令(初期スコープ、これで1周できる教材にする):
  - a接点(常開)、b接点(常閉)
  - 出力コイル
  - 自己保持回路(a接点+コイルで構成できること)
  - タイマ(オンディレイ)
  - カウンタ(アップカウンタ、リセット付き)
  - 内部リレー(補助リレー)
  - 立ち上がり微分(パルス)
  - 並列(OR)/直列(AND)の任意の組み合わせ
- 記法・見た目: メーカー非依存の一般的なラダー記法を基本とする。デバイス名は日本の現場で通じる `X0`(入力)、`Y0`(出力)、`M0`(内部リレー)、`T0`(タイマ)、`C0`(カウンタ)形式を採用する(三菱系に近い表記だが、特定メーカーの製品名・画面デザインは模倣しない)
- ラダー編集UI: 部品パレットから接点・コイル等をグリッドに置き、線でつなぐ。スマホでも操作できることを重視(タップ中心、ドラッグ依存にしない)

### 3.2 学習モード(3段階)
学習は「読む」「直す」「書く」の3段階で構成する。すべて同じシミュレータを使う。

#### (1) 読む
- 完成した回路が表示される
- 「このボタンを押すとどうなる?」という予測問題に答える(選択式)
- 回答後に実際にシミュレータで動かして答え合わせをする
- 狙い: 回路を目で追って動作を予測する力

#### (2) 直す
- 意図した動作をしない回路が表示される(バグは1〜2箇所)
- ユーザーは回路を編集して、仕様通りに動くように修正する
- バグの例: a/b接点の取り違え、自己保持の抜け、タイマ設定値の誤り、インターロック抜け、並列・直列の間違い
- 狙い: 現場で最も頻度の高い「既存回路の不具合を探す」作業に慣れる

#### (3) 書く
- 仕様文だけが与えられる(例: 「押しボタンX0を押すとランプY0が点灯し、3秒後に自動で消灯する」)
- ユーザーは白紙から回路を組む
- 狙い: 仕様からラダーを設計する力

### 3.3 問題と判定
- **判定方式: 入出力の振る舞い一致**。各問題は「入力操作のシーケンス → 期待される出力状態」のテストケースを複数持ち、ユーザーの回路がすべてのテストを通れば正解
- 回路の構造(模範解答との形の一致)は正誤に使わない
- ただし各問題は **模範解答(最適解)** を持ち、正解後に「あなたの回路」と「模範解答」を並べて表示する
- 比較の補助として、ラング数・使用接点数などの軽い指標を模範解答と並べて表示する。指標は正誤に影響させない(「もっと短く書けるよ」の合図として使う)
- 不正解時は、どのテストケースで期待と違ったかを具体的に示す(入力操作と、期待出力・実際出力の差)
- 公式問題は自己保持 → タイマ → カウンタ → インターロック → 組み合わせ、の順に難易度を上げる。各段階で「読む」「直す」「書く」を用意する

### 3.4 サンドボックス
- ユーザーが自由に回路を作って動かせる場
- 回路は保存できる(アカウントに紐づく)
- **回路に自分でテストケース(入力シーケンス → 期待出力)を付けられる**。これにより、サンドボックスで作った回路がそのまま「問題」になる
- 保存した回路は「問題として公開」できる(フェーズ2)

### 3.5 アカウントと進捗同期
- アカウント登録・ログイン(メール or 外部プロバイダ、コスト0円で実現できる方式)
- 未ログインでも「読む」「直す」「書く」の公式問題とサンドボックスは試せる。保存・投稿・ランキング・進捗同期にはログインが必要
- 同期する進捗データ:
  - 問題ごとのクリア状態、クリア日時
  - 問題ごとの試行回数、失敗回数
  - どの問題で止まっているか(最後に挑戦して未クリアの問題)
  - ユーザーが提出した回路そのもの(正解・不正解問わず履歴として保持)
  - サンドボックスの保存回路
- 提出回路の履歴は、後のつまずき分析(管理者ビュー・教材改善)に使う

### 3.6 投稿問題(ユーザー生成コンテンツ)
- サンドボックスで作った回路+テストケースを「問題」として公開できる
- 投稿には模範解答(投稿者の回路)とテストケースが必須。**投稿時に模範解答が自分のテストケースをすべて通ることを自動チェック**し、通らなければ公開できない
- 投稿問題には仕様文(問題文)、難易度(投稿者申告)、タグ(自己保持/タイマ等)を付ける
- 他ユーザーが解ける。解いた人は「いいね」「難易度投票」ができる
- 各問題にクリア率(挑戦者数・クリア者数)を表示する
- 一覧は新着・人気(いいね)・難易度でソート/絞り込みできる
- 通報機能を持つ(不適切投稿の非表示)。初期は自動チェック+通報による事後対応でよい

### 3.7 ランキング
- 学習アプリであることを踏まえ、**速さは競わせない**。指標は貢献・継続系に限定する
  - 解いた問題数
  - 作った問題が解かれた回数
  - 作った問題のいいね数
  - 連続学習日数
- 期間別(週間・月間・累計)で表示
- 組織(§3.8)に所属していれば組織内ランキングも表示

### 3.8 管理者ビュー(組織機能)
- ユーザーは組織を作成でき、招待リンクやコードで他ユーザーを組織に加えられる
- 権限: 管理者 / メンバー
- 管理者はメンバーの以下を閲覧できる:
  - 各メンバーの進捗(どの問題までクリアしたか)
  - つまずき箇所(失敗回数が多い問題、未クリアで止まっている問題)
  - メンバー個別の提出回路の履歴
- 管理者は組織限定の問題(組織内だけに公開する投稿問題)を作れる
- 管理者はメンバーに問題を割り当て(課題)できる。メンバーには「割り当てられた課題」が一覧で見える
- 想定用途: 会社の教育担当者が新人の学習状況を見守る

---

## 4. フェーズ計画と完了条件(DoD)

### フェーズ1: 一人で学べる
含む機能: §3.1 シミュレータ、§3.2 学習モード(読む/直す/書く)、§3.3 判定と模範解答、§3.4 サンドボックス(保存まで)、§3.5 アカウントと進捗同期

DoD:
- [ ] シミュレータが §3.1 の全命令を正しく評価し、ユニットテストで網羅されている
- [ ] スマホのブラウザで回路の閲覧・操作・編集ができる
- [ ] 公式問題が各段階(自己保持/タイマ/カウンタ/インターロック/組み合わせ)× 各モード(読む/直す/書く)で最低2問ずつ、計30問以上ある
- [ ] 振る舞い判定が動作し、不正解時に差分が表示される
- [ ] 正解後に模範解答と指標の比較が表示される
- [ ] サンドボックスで回路を作り、テストケースを付けて保存できる
- [ ] アカウント登録・ログインができ、進捗が同期される
- [ ] コスト0円でデプロイされ、URLで人間が触れる
- [ ] README・PROGRESS・DECISIONS・COST が最新

### フェーズ2: みんなで増やす
含む機能: §3.6 投稿問題

DoD:
- [ ] サンドボックスから問題を投稿でき、投稿時に模範解答の自動チェックが動く
- [ ] 投稿問題の一覧・検索・ソート・絞り込みができる
- [ ] 他人の投稿問題を解け、いいね・難易度投票・通報ができる
- [ ] クリア率が表示される
- [ ] E2Eテストが通っている
- [ ] デプロイ済み、ドキュメント最新

### フェーズ3: 競う・見守る
含む機能: §3.7 ランキング、§3.8 管理者ビュー

DoD:
- [ ] 期間別ランキングが表示される
- [ ] 組織の作成・招待・権限管理ができる
- [ ] 管理者がメンバーの進捗・つまずき箇所・提出履歴を閲覧できる
- [ ] 組織限定問題と課題の割り当てができる
- [ ] E2Eテストが通っている
- [ ] デプロイ済み、ドキュメント最新

### フェーズ4以降(未定・着手しない)
フェーズ3完了後は `docs/PROGRESS.md` に改善候補を列挙して停止し、人間の指示を待つ。

---

## 5. 未決定事項と暫定値

以下は人間が未決定の項目。Claude Codeは暫定値で進めてよい。変更があれば人間が本ファイルを更新する。

| 項目 | 暫定値 |
|---|---|
| アプリ名 | 仮名のまま。ユーザー向け表示は「ラダー図トレーニング」 |
| 対象ユーザー | 製造現場の新人・若手を仮ペルソナとする |
| UI言語 | 日本語のみ |
| タイマの実時間速度 | 実時間。学習用に1x/5x/即時 の切り替えを付ける |
| 問題文の難易度表記 | 5段階(投稿者申告+投票の平均) |
| 認証方式 | コスト0円で実現できる方式をClaude Codeが選択 |
| 投稿問題のモデレーション | 自動チェック+通報の事後対応 |

---

## 6. 技術方針(制約のみ、選択はClaude Codeに委ねる)

- 制約: ランニングコスト0円(§2.3)、スマホブラウザ対応、シミュレータはクライアント側で動作させる(サーバ負荷とコストを避ける)
- 制約: 回路データはJSON等の可搬な形式で表現し、問題・模範解答・テストケース・サンドボックス保存がすべて同じ形式を共有する
- 制約: 判定ロジックとシミュレータは共通のコアを使う(判定用に別実装を作らない)
- 推奨: 型のある言語、テストしやすい構成、無料枠が明確なホスティング・DB・認証サービス
- 具体的な技術選定は `docs/DECISIONS.md` に理由付きで記録する

---

## 7. 用語

- ラング: ラダー図の1行(左母線から右母線まで)
- a接点 / b接点: 常開接点 / 常閉接点
- 自己保持: 出力コイルの状態を自身の接点で保持する回路
- インターロック: 相反する出力が同時にONにならないようにする回路
- スキャン: PLCが全ラングを1回評価する周期
- 振る舞い判定: 回路の形ではなく、入力に対する出力の一致で正誤を決める方式
